[pytest]
testpaths = tests
python_files = test_*.py
addopts =
    -ra
    --strict-markers
    --junitxml=reports/junit-report.xml
    --html=reports/html-report.html
    --self-contained-html
markers =
    smoke: fast tests that validate the QA harness can run end to end

